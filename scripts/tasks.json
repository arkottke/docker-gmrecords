{
    "programName" : "run PS",
    "$schema": "tasks.schema.json",
    "itemName" : "${WORK_ITEM}",
    "tasks": [
        {
            "name" : "test",
            "command" : "pwd && ls -l && df -h"
        }
    ]
}
